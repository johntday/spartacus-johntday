import { ContentfulPreviewConfig } from './contentfulpreview-config';

export const defaultContentfulPreviewConfig: ContentfulPreviewConfig = {
  contentfulPreview: {
    storefrontPreviewRoute: 'cx-preview',
    allowOrigin: 'localhost:9002',
  },
};
